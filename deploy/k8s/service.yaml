apiVersion: v1
kind: Service
metadata:
  name: pulsecheck
  namespace: pulsecheck
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: pulsecheck
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
